For own video mode (like in VirtualBox CustomVideoMode):

1) add the mode to vbetables-gen.c
2) make
3) ln -s VGABIOS-lgpl-latest.stdvga.bin vgabios-stdvga.bin
4) qemu-kvm -vga std -L /<path_to_vgabios_build_folder>/ ......

